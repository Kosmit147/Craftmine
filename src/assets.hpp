/**
 * @file assets.hpp
 * @brief Declares external references to embedded texture data used by the application.
 */

#pragma once

/**
 * @brief Embedded texture data for the blocks texture.
 */
extern const std::span<const std::byte> blocks_texture_data;

/**
 * @brief Embedded texture data for the crosshair texture.
 */
extern const std::span<const std::byte> crosshair_texture_data;

/**
 * @brief Embedded texture data for the exit button texture.
 */
extern const std::span<const std::byte> exit_button_texture_data;

/**
 * @brief Embedded texture data for the exit button hover texture.
 */
extern const std::span<const std::byte> exit_button_hover_texture_data;

/**
 * @brief Embedded texture data for the logo texture.
 */
extern const std::span<const std::byte> logo_texture_data;

// extern const std::span < const std::byte> menu_background_texture_data;

/**
 * @brief Embedded texture data for the start button texture.
 */
extern const std::span<const std::byte> start_button_texture_data;

/**
 * @brief Embedded texture data for the start button hover texture.
 */
extern const std::span<const std::byte> start_button_hover_texture_data;
